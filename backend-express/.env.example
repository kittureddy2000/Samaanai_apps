# Node Environment
NODE_ENV=development
PORT=8080

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/samaanai

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-refresh-token-secret
JWT_REFRESH_EXPIRES_IN=30d

# Email Configuration (SMTP for nodemailer)
# RECOMMENDED: Use SendGrid for ALL environments (local, staging, production)
# Sign up at https://signup.sendgrid.com/ - Free: 100 emails/day

# SendGrid (Recommended - Use same key for all environments)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=SG.your-sendgrid-api-key
APP_NAME=Samaanai

# Alternative Option: Gmail (Only if you really want to, not recommended)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-gmail-app-password

# Option 3: AWS SES (Best for High Volume - $0.10 per 1,000 emails)
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-ses-smtp-username
# SMTP_PASS=your-ses-smtp-password

# Option 4: Resend (Modern, Developer-Friendly - Free: 3,000 emails/month)
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=resend
# SMTP_PASS=re_your-api-key

# Push Notifications (Expo)
# Get token from: https://expo.dev/accounts/[account]/settings/access-tokens
EXPO_ACCESS_TOKEN=your-expo-access-token

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:8080/api/v1/auth/google/callback
GOOGLE_SUCCESS_REDIRECT=http://localhost:8081
MOBILE_GOOGLE_SUCCESS_REDIRECT=samaanai://auth-callback

# Google Cloud (Production)
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# CORS
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:19006