# .env.production.example - PRODUCTION ENVIRONMENT TEMPLATE
# Copy this to .env.production and fill in with REAL values
# NEVER commit .env.production to version control!

# Database Configuration (Use Cloud SQL in real production)
POSTGRES_DB=samaanai_prod
POSTGRES_USER=samaanai_prod_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# JWT Configuration (Generate strong secrets!)
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_GENERATE_STRONG_SECRET_HERE
JWT_REFRESH_SECRET=CHANGE_ME_GENERATE_DIFFERENT_STRONG_SECRET_HERE

# Plaid Configuration (Production credentials)
PLAID_CLIENT_ID=your_production_plaid_client_id
PLAID_SECRET=your_production_plaid_secret
PLAID_ENV=production

# API Configuration
PORT=8080
API_BASE_URL=https://your-domain.com

# CORS (Restrict to your frontend domains)
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com

# Logging
LOG_LEVEL=info

# Email (Production)
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Security
# Rate limiting (requests per minute)
RATE_LIMIT_MAX=100

# Session Configuration
SESSION_TIMEOUT=3600

# IMPORTANT NOTES:
# 1. Copy this file to .env.production
# 2. Replace ALL values with real production credentials
# 3. Use Google Secret Manager for real Cloud Run deployment
# 4. Never commit .env.production to git
# 5. Generate JWT secrets with: openssl rand -base64 32
# 6. Use strong database passwords (32+ characters)
# 7. In production Cloud Run, use Cloud SQL (not containerized postgres)