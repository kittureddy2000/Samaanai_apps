# Docker Environment Configuration
# This file contains default values for running in Docker Desktop
# Copy this to .env if you need to customize

# Note: Database and JWT secrets are configured in docker-compose.yml
# You generally don't need to change anything here for local development